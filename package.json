{
  "name": "neuro-mono",
  "version": "1.0.0",
  "description": "Neural network-based intelligent stereo to mono audio conversion that preserves quality, wideness, volume, and richness",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npx esbuild src/index.ts --bundle --format=iife --global-name=NeuroMono --outfile=dist/index.browser.js",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "audio",
    "stereo",
    "mono",
    "neural-network",
    "audio-processing",
    "dsp",
    "sound"
  ],
  "author": "Alexander P. Scott",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.19",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
